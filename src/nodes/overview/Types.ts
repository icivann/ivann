export const enum OverviewCategories {
  Model = 'Models',
  Data = 'Datasets',
  Train = 'Train',
  Optimizer = 'Optimizer',
}

export enum OverviewNodes {
  ModelNode = 'ModelNode',
  DataNode = 'DataNode',
  TrainClassifier = 'TrainClassifier',
  Adadelta = 'Adadelta',
}
