export const enum OverviewCategories {
  Model = 'Model',
  Custom = 'Custom',
  Train = 'Train',
  Optimizer = 'Optimizer',
}

export const enum OverviewNodes {
  ModelNode = 'ModelNode',
  TrainClassifier = 'TrainClassifier',
  Adadelta = 'Adadelta',
}
