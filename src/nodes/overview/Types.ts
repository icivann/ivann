export enum OverviewCategories {
  Model = 'Model',
  Data = 'Data',
  Custom = 'Custom',
  Train = 'Train',
  Optimizer = 'Optimizer',
}

export enum OverviewNodes {
  ModelNode = 'ModelNode',
  DataNode = 'DataNode',
  TrainClassifier = 'TrainClassifier',
  Adadelta = 'Adadelta',
}
