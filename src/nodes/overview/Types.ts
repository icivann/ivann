export enum OverviewCategories {
  Model = 'Model',
  Data = 'Data',
  Custom = 'Custom',
  Train = 'Train',
  Optimizer = 'Optimizer',
  Loss = 'Loss',
}

export enum OverviewNodes {
  ModelNode = 'ModelNode',
  DataNode = 'DataNode',
  TrainClassifier = 'TrainClassifier',
  Adadelta = 'Adadelta',
  Nllloss = 'NLLLoss',
}
