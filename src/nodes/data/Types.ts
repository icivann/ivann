export const enum DataCategories {
  Custom = 'Custom',
  IO = 'I/O',
  Transform = 'Transform',
}

export const enum DataNodes {
  InData = 'InData',
  OutData = 'OutData',
  ToTensor = 'ToTensor',
  Grayscale = 'Grayscale',
}
