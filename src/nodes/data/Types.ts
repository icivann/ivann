export enum DataCategories {
  Custom = 'Custom',
  IO = 'I/O',
  Transform = 'Transform',
}

export enum DataNodes {
  InData = 'InData',
  OutData = 'OutData',
  ToTensor = 'ToTensor',
  Grayscale = 'Grayscale',
}
