import Vue from 'vue';
import Vuex from 'vuex';
import EditorType from '@/EditorType';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    editor: 0,
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
